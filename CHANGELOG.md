# 更新日志

## v1.0.32（2026-09-15）

- **适配 `@linkdesk/ui` 0.2.0 的类名归一**：共享组件的裸类名（`badge` / `button` / `combobox` / `mdv` / `selectbox` / `sle` / `slider` / `toggle`）全部带上 `ldk-` 前缀——它们此前在「宿主 + 共享组件 + 所有已加载插件」同一张样式表里是**全局标识符**，通用英文词极易被插件自己的元素撞上。本插件对共享控件的两处 scoped 调优同步改名：`.mpd-chg .mdv` → `.mpd-chg .ldk-mdv`、`.mpd-title-row .badge .codicon` → `.mpd-title-row .ldk-badge .codicon`
- 这两行不改就是**静默失配**——旧的裸选择器不再命中任何元素，更改日志正文的上边距与「官方发布」徽标里的图标尺寸/基线会悄悄跑掉，没有报错
- `@linkdesk/ui` 升到 `^0.2.0`（`^0.1.4` → `^0.2.0`）。**必须手动放宽区间**：0.x 的 caret 只在上界之内挑版本，`^0.1.4` 永远够不到 0.2.0
- 无功能变化

## v1.0.31（2026-09-15）

- **分发件补上 MIT LICENSE**：`LICENSE` 早就在本仓里（E6#108g 那批加的），但**已发布的那版产物比它早** ⇒ 用户手上那份 zip 里一直没有版权声明。MIT 要求「副本里带声明」，而 zip 才是用户真正拿到的那份
- **不再夹带仓库面文件**：`@linkdesk/plugin-sdk` 升到 0.1.19（^0.1.16 → ^0.1.19）——旧 SDK 的打包通道会把 `marketplace.json` / `scripts/ci-verify.mjs` / `AGENTS.md` 这类**仓库面文件**一起装进 zip（那是给仓库看的，不是给用户看的），0.1.19 的排除表已覆盖
- 无功能变化——本版只为让「用户拿到的产物」与仓库对齐

## v1.0.30（2026-09-14）

- 修好市场列表里「同一个插件两张脸」：列表行此前读的是目录条目里的 `icon`，而图标栏那几只插件（文件树 / 串口监视器 / 设置 / 插件市场）的 `icon` 按设计是单色线稿（白线剪影），于是市场里显线稿、装到本地显彩色身份图。现在列表行与详情页同序裁决——**已装读包内身份图，未装读目录条目身份图**（目录条目侧由发布链路自动带上 `marketIcon` 的绝对 URL）
- 已装插件的图标改回**本地**读（快、断网可用），不再可能去拉远程图；未装插件走目录里的绝对 URL——两态各自归位
- 禁用列表行的图标同样改为「插件自己的图优先」（此前只能退到目录条目取图）

## v1.0.29（2026-09-14）

- 源码迁入独立仓（E6#99，L7 第 7.2 轮）——从壳仓 `Encaron/linkdesk` 抽出本插件子树，历史全保（hash 变）
- 随包 `plugin.json` 显式声明 `pluginId`（E6#98g）：插件身份不再靠目录名兜底，独立仓构建出的包名与身份稳定
- `$schema` 改指本仓 `node_modules/@linkdesk/plugin-sdk`（脱离壳仓后原相对路径指到仓外，编辑器补全/校验会失效）


## v1.0.28（2026-09-11）

- 补充 README 与场景封面（此前全仓唯一没有 README 的代码插件，详情页「详情」页签是空的）；「更改日志」页签已装分支的块头改为诚实说法——下面铺的是全部历史，不再自称单一版本

## v1.0.27（2026-09-11）

- 这一版你同样看不出任何区别——纯内部整理，界面、按钮、位置、文字一字未变。市场插件里有几个代码文件长到了一千多行（最长的那个插件详情页），一个文件里塞了太多不相干的东西，改一处容易碰坏另一处。这一版把它们按职责拆成了一批小文件、分门别类收进同名文件夹里。之所以还是记一条，是因为商店会显示版本历史——免得你看到一个没有说明的新版本号，以为漏看了什么

## v1.0.26（2026-09-11）

- 「勾了自动更新，重开软件还是不装」的另一半也修好了（E6#83）。上一版修的是「那趟检查可能白跑一趟、白跑了也没有下一次」；这一版修的是**另一回事**——检查跑通了，却被一条**你看不见的记录**拦住了。事情是这样：你从版本下拉里挑着装一个旧版，软件会悄悄记一条「他就要停在这一版，以后别给他自动升」。这条记录**界面上一个字都不显示**，可它会一直拦住自动更新——于是你把「自动更新」勾上、关软件、重开，什么都没发生，而那个勾选框鼠标停上去写的是「勾选后自动更新——有新版本就自动装上」。**两条都是你亲手下的指令，软件一声不响让那条看不见的赢了。**现在改成：**你勾上「自动更新」的那一刻，之前挑的旧版就不算数了**——立刻查、有新版本立刻装，装完照常发通知告诉你为什么版本变了。反过来不成立：你只是挑个旧版看看，软件**不会**顺手把你勾的「自动更新」关掉——那是两回事，不替你做主
- 另外，那条「你停在哪个版本」的记录**现在看得见了**——插件详情页「自动更新」勾选框旁边会显示一枚「已停在 v0.1.0」。以前它藏得连个影子都没有，你不点、不问、不翻文件根本不知道它存在；一个能拦住你半天的事，至少得让你看得见它。它只是个说明，不能点——想解除的话，勾上「自动更新」，或者在下拉里选最新版，都行

## v1.0.25（2026-09-11）

- 「勾了自动更新，重开软件却不自动装」修好了（E6#82）。原因不在开关、也不在判断，在**时机**：软件打开后约十秒会检查一次有没有新版，而那次检查**一辈子只有一次机会**——它要上网拉商店目录、要读你装了哪些插件，这两件事只要当下有一件没成（刚开机网络还没热、代理还没起来），检查就**一声不响地放弃**，这一次开机就再也不会补了。于是你看到的就是：右下角那条「有新版本」还在，自动更新却一整天没动静。现在改成：**没检查出结果就换个时间再查**（十秒、三十秒、一分钟、两分钟、五分钟各一次），只要拿到结果就停——不会反复打扰你。实机验证：把一个插件降回旧版、当场跑一次这个检查，它立刻自动装回了新版并发通知；这一版修的就是「那次检查可能白跑一趟、而白跑了也没有下一次」

## v1.0.24（2026-09-11）

- 这一版你看不出任何区别（E6#81）——只是把市场内部一本「谁有新版本」的记事本拆了。那本本子是早期留下的：当时「可更新」提示和自动更新都靠它，后来这两件事都改成当场算（查一次盘上装的是哪版、比一下商店里的版本），本子就只剩下有人往上面写、没人再翻。拆掉之后自动更新、可更新提示、版本号显示全部照旧。之所以还是记一条，是因为商店会显示版本历史——免得你看到一个没有说明的新版本号，以为漏看了什么

## v1.0.23（2026-09-11）

- 勾上「自动更新」以前有可能等于白勾（E6#81）：你打开某个插件的详情页、把「自动更新」打开，如果软件这时候**还没查过**（打开软件后大约十秒才自动查一次），这一勾就什么都不会发生——不装、界面上也一个字不提，得等下一轮检查才补上。现在改成：勾的那一下，软件**当场去查**这个插件有没有新版，有就立刻装，装完或装失败都发一条通知。这也正是 1.0.20 那条说明里写的「勾上开关那一刻如果正好有可更新的版本，立刻装，不用等下一轮检查」——此前其实没做到，现在做到了

## v1.0.22（2026-09-11）

- 还有一条你看不见但它会咬人的（E6#81）：以前你挑着装了个旧版（等于告诉软件「我就停在这版，别自动升」），然后把这个插件卸掉、再从商店装回最新版——那条「停在旧版」的旧命令还留着，于是自动更新就安安静静地再也不管它了，界面上一个字都不说。现在卸载会把这类旧账一起划掉（重新装一次就是你新的决定）。「自动更新」那个开关本身照旧保留——那是你想让这个插件自动更新的意思，跟你装没装过它是两回事

## v1.0.21（2026-09-11）

- 版本下拉框改成显示你「现在装的是哪个版本」（E6#81）——以前它显示的是「可以更新到哪个版本」，所以你明明装着 0.1.0，下拉框却写着 0.1.1，看起来像你已经装上了新版。现在下拉框说人话：你手上是什么它就写什么。旁边那个「更新到 vX」的按钮一点没变——想更新还是一键，不用先去下拉框里挑一圈；当然你想装回旧版或试 beta，下拉框照样能挑
- 顺手堵掉一个漏网的假按钮（E6#81）：随软件一起发的那批插件你自己是换不动版本的（它们住在软件安装目录里，更新引擎不认），可之前只要商店里登了它们的历史版本，详情页照样画一个版本下拉——你一挑，旁边就冒出一个「更新到 vX」，点下去必然失败。现在这类插件干脆不画这个下拉，也不画那个按钮。上一版只挡住了「自己冒出来」的那颗按钮，这回连手动挑出来的路也堵上了

## v1.0.20（2026-09-11）

- 自动更新恢复运转（E6#79）——1.0.8 那条「自动更新暂停」是**误读**：当时你说的是「安装确认那个弹窗每次都问」，跟自动更新没有关系，是软件把这条规矩自己推到了自动更新上。现在纠正过来：确认卡只管你**亲手点**「安装」/「更新」那一下；勾上「自动更新」这个开关，本身就是你为后台那条路签的字，它照常装、不再被确认卡挡。装完会发一条通知告诉你更新到了哪个版本——不会让你回来发现版本变了却不知道是谁动的手；没装成也会发一条并指路可手动重试（更新失败不静默）。勾上开关那一刻如果正好有可更新的版本，立刻装，不用等下一轮检查

## v1.0.19（2026-09-11）

- 详情页能打开插件的文件夹了（E6#78）：插件**装好后**，「大小」那一行的数字变成可点——点一下用资源管理器打开这个插件装在哪儿（鼠标停上去显示完整路径）。以前这一行只是个数字，看得见体积、找不到它在哪。**没装的时候还是普通数字**——不给一个点下去必然报错的入口；插件**真存过数据**（导出文件、下载物）时右栏多一行「数据位置」同样可点，**没数据的插件不显示这一行**（不给一个点开是空文件夹的入口，跟 VS Code 同一判据）。这一行不新开行、不换文案——只把数字变蓝，所以装/没装之间切换时版式不会跳

## v1.0.18（2026-09-11）

- 插件详情页的「仓库 / 打开仓库」不再指错地方（E6#77）：以前这一行点开的是**商店自己的仓库**，不是插件作者的家——因为软件把「这条是从哪个市场源列出来的」当成了插件主页，官方目录里所有插件都指向同一个仓库（比如 hello-linkdesk 点「仓库」跳去了 linkdesk-marketplace）。现在改成认插件自己的地址；认不出来（比如托管在 CDN 上）就**不显示这一行**——不给一个指错路的链接。作者也可以在目录里显式写一条 `repository` 指定自己的主页。「来源」这一行不变，它说的是「从哪个商店货架看到这条」，跟插件主页是两回事——两边并排出现时各说各的，不再打架

## v1.0.17（2026-09-11）

- 卸载插件现在会出现在通知面板的安装任务里（E6#73m）：此前卸载一个插件全程没有任何进度，卸载大插件时不知道是卡住了还是在跑；现在显示「卸载中...」。卸载无法中断（正在删文件），所以这一行**不提供**「取消安装」按钮——不给一个按不动的按钮

## v1.0.16（2026-09-11）

- 更新这块补齐「跟安装一样的待遇」（E6#73j）：①更新终于有进度了——此前更新全程只有按钮上「更新中...」三个字，大包几十秒里看不出是卡死还是在跑，现在和安装共用同一套进度行（阶段短语 + 真百分比 + 可以取消）；②更新失败不再 8 秒自己消失——和安装失败一样常驻并带 [重试]；③「有新版」的提示不再一次刷十几条——同一批合成的插件现在只发一条汇总（「4 个插件有新版本：A、B、C 等」），之前每个插件一条、十几条堆在一起反而什么都看不见；④最后是随软件一起发的那批插件不再画一个点下去必然失败的「更新到 vX」按钮——它们不在可更新区，更新会被引擎拒绝，这个按钮属于骗人；⑤更新成功后商店里的版本号立刻变新，不用重启软件才收敛

## v1.0.15（2026-09-10）

- 安装状态不再「卡住」（E6#73i）——此前如果在插件安装期间把「插件详情」标签页关掉、同时把「探索插件」侧栏收起来，市场的安装状态就停止更新了；之后重新打开，徽标会一直停在「安装中」不动（其实早就装完了）。现在重新打开那一刻就拿到最新状态，不会再停在旧画面上

## v1.0.14（2026-09-10）

- 通知归组修好（E6#73g）——市场发出的通知此前没有署名，全部落进通知面板的「其他」大杂烩里；现在市场自报家门，安装进度、安装失败、安装完成、版本提醒、自动更新暂停说明统统归到「插件市场」一组，一组一标题、一组一份常驻配额，不再和别人的消息混在一堆；纯展示归属，不改任何安装行为

## v1.0.13（2026-09-10）

- 并发安装（E6#73c 第 2 步）：一个接一个的点「安装」不再需要排队等前一个——同一时刻最多同时装 3 个，其余照常排队并在通知面板里明确显示「等待安装中」；排队、上限、同插件去重、卡死释放全部由软件底层统一管理，市场不再自己攥着一道「同一时间只能装一个」的闸；行内状态（安装中 / 等待安装中 / 失败 + [重试]）与通知面板的安装条目读同一份数据，两处永远不会打架；副作用一处：侧栏「从本地安装插件」按钮不再显示「校验中/复制中」这类阶段词（那句话在并发下会显示成别的安装的阶段，属撒谎），只保留「安装中...」

## v1.0.12（2026-09-10）

- 通知面板安装进度改由软件底层统一表达（E6#73d）——市场自画的右下角进度条整段拆除：同一面板里两条进度 = 同一次安装显示两遍

## v1.0.11（2026-09-10）

- 安装文案归一（E6#73h）：一次安装不再弹两条「已安装」（成功只由软件底层发一条，市场不再自己发）

## v1.0.10（2026-09-10）

- 安装请求不再被静默丢弃（E6#73c 第 1 步）：此前一个插件正在安装时点另一个 = 点了等于没点；现在照收并画「等待安装中」回执（当时仍是排队串行）

## v1.0.9（2026-09-10）

- 安装失败不再说谎（E6#73e）：①下载地址失效（404/403 这类）与服务器暂时打嗝（500/503）拆成两句不同的话——此前一律报「网络连接不可用」，于是你会去查网络、反复重试，而真相是那个地址已经没了，重试的其实是另一个插件；②失败通知现在带上插件名和引擎原话（认不出原因时直显原文，不再用「未知错误」糊过去）；③下载挂死有救——30 秒收不到任何新字节即判超时并自动重试（多试 2 次），失败留 [重试]，不会再出现「进度条卡住、既没结果也没得点」

## v1.0.8（2026-09-10）

- 安装确认改回「每次都问」（E6#71k 修订，当日推翻 1.0.7）——用户拍板：每次安装/每次更新都弹确认卡，官方目录也不豁免；1.0.7 的「每来源一次」信任表与台账整套删除（没有「记住谁被信任过」，就没有「记错」这件事），市场源弹窗的「已信任的来源」撤销列表随之移除；失败后的 [重试] 也要过一次确认门——重试不是免问券；同一插件 ID 换来源、http 明文源天然每次重问；「自动更新」暂停（自动更新发生在你不在场时，弹不出确认卡就不能装）——勾选框保留意愿，勾上时当面说明为什么没动静，有新版本照常发通知让你手动更新；本机制只在市场 UI 层生效，不宣称更安全

## v1.0.7（2026-09-10）

- 安装确认改「每来源一次」（E6#71k）——同一来源（owner/repo）只询问一次：官方源预信任零弹卡；第三方源首次安装弹一张富确认卡，确认后该来源不再询问（装 7 个插件从 7 张卡降到 1 张）；市场源弹窗补「已信任的来源」列表可逐个撤销（撤销后该来源下次安装重新询问）；同一插件 ID 换来源发布强制重问；http 明文源不记忆、每次询问；后台自动更新不弹卡——来源未信任时跳过并提示去详情页手动确认；本机制只在市场 UI 层生效，不宣称更安全

## v1.0.6（2026-09-10）

- 安装反馈收尾（E6#71g/71i/71j）：①失败 toast [重试] 不再依赖市场落地页打开——marketplace 命令组注册自 index.tsx 迁 marketplaceShared 模块顶（任意市场视图/详情激活即注册，此前详情页触发失败时壳命令占位缺失致 [重试] 无反应）；②安装进度条成真（71i）——进度 toast 更新携 0-100 真百分比驱动确定进度条（下载段），其余阶段不定态扫动；③安装失败通知长驻不自动消失（71j）——归因诊断 + [重试] 等用户决定，手动点 ×（壳上限内常驻互汰）

## v1.0.5（2026-09-10）

- 安装入口行为归一（E6#71d）：侧栏「探索插件」行内安装补富内容确认——与详情页同款先弹确认卡（installConfirmPayload + ConfirmInstall 视图，壳 DialogHost 容器），确认才装、取消不装——两安装入口从「一处直装一处确认」的不一致清零（mockup 02 两入口都先确认本意落地）

## v1.0.4（2026-09-09）

- 安装确认弹窗归位壳 Dialog（E6#71c）：市场自绘富内容确认卡迁入壳 DialogHost content 槽（居中+全屏遮罩+Esc+焦点锁由壳给，卡片排版/按钮仍市场 ConfirmInstall 视图自画）——装/卸/降级三确认共用一个壳 Dialog 容器

## v1.0.3（2026-09-09）

- 更新失败归因补齐 + 原文直显（E6#71b）：分类表认识更新域状态/策略错（conflict 分支补上），认不出时失败 toast 直显引擎原文（如「不在用户安装区」）——终结「更新失败：未知错误」黑洞

## v1.0.2（2026-09-09）

- 详情页说明区 README 随包媒体 assetBase 接线（E6#70a 数据链路）

## v1.0.1（2026-09-09）

- 新增 marketIcon=resources/icon-brand.svg（E6#69 Type-2 彩色身份图）——icon 保持线稿店招（Type-1）仅图标栏用；市场展示位转用品牌块
